# 2026-07-03 작업 일기 (밤 작업 — 원래 07-04 파일에 기록했다가 분리)

## 세션 1
- **한 일**: 프로젝트 자율 분석 → 문서 5종 생성
  - PROJECT.md — 프로젝트 사실 기록 (구조·동작·데이터 흐름)
  - DIAGNOSIS.md — 기술 진단 (보안/버그/기능/위생, 우선순위 P1~P4)
  - CONTEXT_REVIEW.md — 채용 맥락 평가 (채용 심각도 표 포함)
  - PLAN.md — 통합 실행 계획 (T1~T16, 승인 체계·실행 순서 포함)
  - CAPTURE_PLAN.md — 블로그용 캡처 계획 (촬영 세션 ⓪①②, 간소판으로 압축)
- **바꾼 파일**: 위 문서 5종 신규 생성 + CLAUDE.md에 "세션 작업 규칙" 추가. 코드 수정·커밋 없음.
- **검증 결과**: 코드 미변경이라 빌드 검증 해당 없음. 진단 과정에서 npm audit(취약점 16건 확인), npm run lint(경고 1건), 시크릿 grep·git 이력 검사(깨끗함) 실행함.

## 세션 2
- **한 일**: CAPTURE_PLAN.md 기준 "before" 캡처 진행
  - T3(의존성 취약점), T5(챗봇 맥락 없음), T6-c(배너 오표시, T5와 겸용), T8(`/api/health` 정보 노출), T9(로그에 대화 노출) — 캡처 완료 (이지캡쳐 사용)
  - T2(rate limit 없음) — 실제 부하 테스트 대신 route.js 코드(검증 로직 부재) 캡처로 대체 결정 (실비용·텔레그램 알림 방지)
  - T6-a(스크롤 사망)·T6-b(id 중복)·T7(라이트 테마 미저장) — 임팩트 낮다고 판단, 기록 생략하고 코드만 수정하기로 결정
  - 진행 중 route.js 확인하며 T9 노출 항목(벡터스토어 ID는 응답에 없음/`file_id`는 있음)·T2 관련 코드(rate limit·middleware 전무 확인) 등 사실관계 점검
- **바꾼 파일**: 없음 (코드 읽기만 함: `app/api/health/route.js`, `app/api/sendMessage/route.js`). 캡처 이미지는 `local-notes/captures/`에 사용자가 직접 저장.
- **검증 결과**: 캡처 작업이라 빌드 검증 해당 없음.
- **다음에 할 일**: route.js(T2·T5·T8·T9) 수정, package.json axios 제거(T3), 프론트 T6-a/b·T7 수정 → 이후 "중간"(코드 diff)·"후"(재현 검증) 캡처 진행.

## 세션 3
- **한 일**: T1(실제 이력서 교체) 완료
  - 사용자가 `public/`에 넣은 `김윤성 이력서 v0.pdf` → `kim-yunsung-resume.pdf`로 리네임
  - 기존 `sample-resume.pdf`는 이미 없는 상태였음(별도 삭제 불필요)
  - Header.jsx 다운로드 링크를 새 파일명으로 수정 + `download="김윤성_이력서.pdf"` 속성 추가(저장 파일명은 한글로 표시)
  - dev 서버 없어 `.claude/launch.json` 신규 생성(dev, port 3000)
- **바꾼 파일**: `public/kim-yunsung-resume.pdf`(신규), `public/sample-resume.pdf`(삭제), `app/components/Header.jsx`, `.claude/launch.json`(신규)
- **검증 결과**: dev 서버 기동 후 `curl http://localhost:3000/kim-yunsung-resume.pdf` → `200 OK`, `application/pdf`, 131720 bytes(실 파일 크기 일치) 확인. 화면 스크린샷으로 버튼 표시 정상 확인.
- **커밋**: 5cd733c "실제 이력서 파일로 교체 (T1)" — push는 보류(사용자 동의 대기).
- **다음에 할 일**: CLAUDE.md "알려진 이슈"의 sample-resume.pdf 항목 제거(승인 대기), T4(증빙 문서 연결 방식 결정) 또는 T2 route.js 합의 세션 진행.

## 세션 4 (세션 3에서 이어짐)
- **한 일**: T4(증빙 문서 연결) 프로토타입 완료
  - 사용자가 `local-notes/docs/`에 문서 5종 전달 (docx 3 + md 2)
  - docx 3건 개인정보 점검(전화·이메일·주민번호 패턴 검색 → 없음, RedTom21@HotMail.com은 dgrep 악성코드 IOC로 정상)
  - Word COM으로 docx→PDF 변환, `public/reports/`에 배치 (snort-ids-report / malware-report-cean-agenttesla / malware-report-dgrep)
  - assets.js에 attachments 배열 추가(카드1: 보고서 2건, 카드3: dgrep 1건), Projects.jsx에 "증빙 문서" 렌더링 블록 추가
  - md 노트 2건(bton02·템플릿)은 프로토타입에서 제외 — 추후 GitHub 연결(C안) 검토
- **바꾼 파일**: `assets/assets.js`, `app/components/Projects.jsx`, `public/reports/*.pdf`(신규 3건)
- **검증 결과**: dev 서버 스크린샷으로 버튼 표시 확인, curl로 3건 모두 200/application/pdf 확인. PDF 서식 품질은 사용자 확인 대기.
- **미커밋**: T4 변경분 전체 (사용자 PDF 품질 확인 후 커밋 예정)

---

## 이날 기준 전체 요약 (인수인계용 — 이후 상태는 2026-07-04.md 참고)

### 완료
- **T1 이력서 교체** ✅ — `public/kim-yunsung-resume.pdf`, Header.jsx 링크 수정. 커밋 5cd733c. **push는 안 함**
- **T4 증빙 문서 연결** 🔶 프로토타입만 완료, **미커밋**
  - 완료: PDF 3건 변환·배치(`public/reports/`), assets.js `attachments` + Projects.jsx "증빙 문서" 버튼 구현, dev 서버 검증(200/PDF)
  - **잔여 ① 커밋**: 사용자가 PDF 서식 품질(브라우저 열람) 확인 후 커밋
  - **잔여 ② 프론트엔드 개선**: 현재는 최소 구현(버튼 나열) — 애자일 방식으로 디자인·UX 개선 예정
  - **잔여 ③ 미첨부 파일 추가 구현**: md 노트 2건(`local-notes/docs/`의 "bton02 분석 노트.md", "악성코드 분석 노트 템플릿.md")은 아직 사이트에 연결 안 됨 — GitHub 연결(C안) 검토, T13(저장소 위생) 선행 필요
- `.claude/launch.json` 신규 생성 (dev 서버 preview용)

### 이날 기준 남은 작업 (PLAN.md T번호 기준)
- **T2** sendMessage 방어선 / **T3** 의존성 정리 / **T8** health 노출 축소 → **2026-07-04 세션에서 처리됨**
- **T5** 챗봇 맥락+추천 칩+실패 안내 / **T6** 챗봇 UI 버그 3종 / **T7** 테마 저장 버그 / **T9** 서버 로그 정리
- **T10** 대화 수집 고지 / **T11** SEO·OG / **T12** 히어로·Skills 문구 / **T13** 저장소 위생 / **T14** 콘텐츠 단일 소스화
- **T15** 연락 경로 확장 / **T16** 접근성·잔여 정리
- **기타**: CLAUDE.md "알려진 이슈" sample-resume 항목 제거(승인 대기), push 여부 결정
